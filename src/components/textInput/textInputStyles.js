import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    borderWidth: 1,
    borderRadius: 4,
    width: "100%",
    marginBottom: 5,
    borderColor: "#7f8280",
    height: 40,
    paddingLeft: 10
  }
});

export default styles;
