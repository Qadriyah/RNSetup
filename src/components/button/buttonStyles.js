import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginBottom: 5,
    borderRadius: 4,
    borderWidth: 1,
    borderColor: "green"
  }
});

export default styles;
